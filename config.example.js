// Copy this file to config.js and fill in your own values if needed.
// config.js is gitignored, so your secrets stay local.

const CONFIG = {
    // Leave empty unless you want to use real weather data for live events
    WEATHER_API_KEY: '',

    // Game tuning values (safe to change)
    SUPERCARD_MULTIPLIER: 3.0,
    WEEKLY_DECAY: 5,
    RIVAL_PROMO: 'SYNDICATE PRO',
    ALT_PROMO: 'ELITE GLOBAL'
};

export default CONFIG;
